% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cube_adi.R
\name{cube_adi_solve}
\alias{cube_adi_solve}
\title{Solve a Cube With a Trained Network}
\usage{
cube_adi_solve(net, state, budget = 200L, use_policy = TRUE, batch_size = 256L)
}
\arguments{
\item{net}{A trained \code{cube_adi_net}}

\item{state}{Integer vector, the scrambled state}

\item{budget}{Most moves to try before giving up}

\item{use_policy}{Break ties with the policy network's ranking}

\item{batch_size}{Batch the networks were trained with. Scoring reuses the
compiled graph at this size, padding the 24 children out to fill it.}
}
\value{
List with \code{solved}, \code{path} (move names) and \code{values}
}
\description{
Walks downhill on the value network, one forward pass per move, refusing to
revisit a state it has already stood on. Banning only the inverse of the last
move is not enough -- four of the same quarter turn also return to where they
started -- so the walk remembers where it has been, which covers every length
of cycle with one rule.
}
