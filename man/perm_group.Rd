% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perm_group.R
\name{perm_group}
\alias{perm_group}
\title{Create a Permutation Group from a Table of Moves}
\usage{
perm_group(perms, n = NULL, name = "perm_group")
}
\arguments{
\item{perms}{Named list of integer vectors, each a permutation of \code{1:n}
read as \code{new[i] <- state[perm[i]]}. Names become the move names.}

\item{n}{Integer, the state length. Defaults to the length of the first
permutation.}

\item{name}{Character, a label for printing (default \code{"perm_group"})}
}
\value{
An object of class \code{perm_group}
}
\description{
Declares a group by listing its moves as explicit permutations. This is the
general constructor: any puzzle whose moves permute a fixed set of positions
can be expressed here, with no C++ involved.
}
\examples{
# the 3x3x3 cube, from the table the package already ships
g <- perm_group(cube_moves(3), n = 54, name = "cube3")
g

# a group of your own: two generators on five points
g2 <- perm_group(list(
  a = c(2L, 3L, 4L, 5L, 1L),
  b = c(2L, 1L, 3L, 4L, 5L)
))
group_moves(g2)
}
\seealso{
\code{\link{topspin_group}}, \code{\link{cube_group}},
\code{\link{perm_group_intro}}
}
