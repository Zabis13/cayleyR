% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cube_nnn.R
\name{cube_layer_move}
\alias{cube_layer_move}
\title{A Single Layer Turn, by Axis and Layer}
\usage{
cube_layer_move(n, axis, layer, turns = 1L)
}
\arguments{
\item{n}{Integer, the side of the cube}

\item{axis}{Integer, 1, 2 or 3 for the x, y and z axes. x runs L to R, y
runs D to U, z runs B to F.}

\item{layer}{Integer in \code{1:n}, which layer along that axis turns.
Layer 1 is at the negative end, layer \code{n} at the positive one.}

\item{turns}{Integer, quarter turns clockwise seen from the positive end of
the axis: 1, 2 or 3. Half turns are available here even though they are
not in the alphabet.}
}
\value{
Integer vector of length \eqn{6n^2}, the permutation
}
\description{
The generator's own vocabulary, for when naming a layer is less convenient
than pointing at it --- on a 5x5x5, say, where the inner layers have no
letters anyone agrees on.
}
\examples{
# on a 3x3x3, layer 3 about y is the U face
identical(cube_layer_move(3, axis = 2, layer = 3, turns = 3),
          cube_moves(3)[["U"]])

# layer 2 about x is the M slice
identical(cube_layer_move(3, axis = 1, layer = 2, turns = 1),
          cube_moves(3)[["M"]])
}
\seealso{
\code{\link{cube_moves}}
}
