% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perm_group.R
\name{group_order}
\alias{group_order}
\title{Order of a Move Sequence in a Group}
\usage{
group_order(group, moves, max_reps = 100000L)
}
\arguments{
\item{group}{A \code{\link{perm_group}}}

\item{moves}{Move sequence: names, indices, or a space-separated string}

\item{max_reps}{Integer, cap on repetitions before giving up (default 100000)}
}
\value{
Integer, the order, or \code{NA} if \code{max_reps} was reached
}
\description{
How many times a word must be repeated before the state returns to where it
started. This is the order of the word as a group element, and does not
depend on the starting state.
}
\examples{
g <- cube_group(3)
group_order(g, "R")
group_order(g, "R U")
group_order(g, "R U R' U'")
}
\seealso{
\code{\link{group_compose}}
}
