% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_walk.R
\name{generate_walk}
\alias{generate_walk}
\title{Random Walks From the Solved State, With the Moves They Took}
\usage{
generate_walk(group, n = 1L, n_moves = 10L, exact = TRUE, no_undo = TRUE)
}
\arguments{
\item{group}{A \code{\link{perm_group}} to walk in}

\item{n}{Number of walks (default 1)}

\item{n_moves}{Length of each walk, or the longest one when
\code{exact = FALSE} (default 10)}

\item{exact}{Walk \code{n_moves} exactly (default \code{TRUE}), or draw each
length uniformly from 1 to \code{n_moves}}

\item{no_undo}{Refuse a move that immediately undoes the previous one
(default \code{TRUE})}
}
\value{
A list with
\describe{
\item{\code{states}}{integer matrix, \code{n} rows, one walk endpoint
per row}
\item{\code{depth}}{integer vector, the length of each walk}
\item{\code{moves}}{list of \code{n} character vectors, the word each
walk took}
\item{\code{solution}}{list of \code{n} character vectors, each the
inverse of the corresponding word --- a path from that state to
solved}
}
}
\description{
Walks away from the identity by random moves and reports the word it walked,
which is what separates this from \code{\link{generate_state}}. That function
hands back a state and forgets the route; this one hands back the route as
well, and the route is a way home --- \code{\link{invert_path}} of it solves
the state.
}
\details{
That matters wherever a solver is measured rather than merely run. Asking
where an estimator ranked the helping move, or at which step a search dropped
the trail, needs a state whose answer is known in advance, and the only cheap
way to know one is to have walked there.

Two switches, because generating states for training and generating them for
measurement pull opposite ways:

\describe{
\item{\code{exact}}{\code{TRUE} (default) walks \code{n_moves} every time,
which is what measurement wants: a result is then about that depth and
not about a mixture. \code{FALSE} draws a length uniformly from 1 to
\code{n_moves}, the sampling training asks for, spreading accuracy
outward from the goal without weighting the loss.}
\item{\code{no_undo}}{\code{TRUE} (default) refuses a move that undoes the
one before it. \code{generate_state} allows those, so its \code{n_moves}
is an upper bound on the distance --- a drawn \code{U U\'} quietly costs
two moves and travels nowhere. Refusing them makes the label mean more.
Longer cycles are left alone: four quarter turns of one face also come
home, and refusing every way back would mean searching the group rather
than walking it.}
}

Even with \code{no_undo} the length stays an upper bound on the true distance
to solved: a shorter route may exist that this walk did not take. A solver
compared against \code{solution} is therefore never credited with a shortcut
the reference did not have, which is the safe direction to err in.
}
\examples{
g <- cube_group(3)
w <- generate_walk(g, n = 2, n_moves = 6)
dim(w$states)
w$moves[[1]]

# the reported solution really does solve the state
s <- group_apply(g, w$states[1, ], w$solution[[1]])
identical(s, group_identity(g))

# for training: a spread of depths rather than one
w <- generate_walk(g, n = 5, n_moves = 10, exact = FALSE)
w$depth
}
\seealso{
\code{\link{generate_state}} when the route is not needed,
\code{\link{invert_path}}, \code{\link{cube_group}}
}
