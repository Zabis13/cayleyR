% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perm_group.R
\name{group_inverse_seq}
\alias{group_inverse_seq}
\title{Invert a Move Sequence}
\usage{
group_inverse_seq(group, moves, as_names = TRUE)
}
\arguments{
\item{group}{A \code{\link{perm_group}}}

\item{moves}{Move sequence: names, indices, or a space-separated string}

\item{as_names}{Logical, return names rather than indices (default
\code{TRUE})}
}
\value{
The inverse sequence, as names or indices
}
\description{
The word that undoes the given one: reversed, each move replaced by its
inverse. Errors if some move's inverse is not in the group's alphabet.
}
\examples{
g <- cube_group(3)
group_inverse_seq(g, "R U R' U'")

# a word and its inverse cancel
s <- group_apply(g, group_identity(g), "R U U F'")
identical(group_apply(g, s, group_inverse_seq(g, "R U U F'")), group_identity(g))
}
\seealso{
\code{\link{group_apply}}
}
