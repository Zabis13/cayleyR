% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cube_santa.R
\name{cube_santa_perm}
\alias{cube_santa_perm}
\title{Sticker Relabelling Between Santa 2023 and This Package}
\usage{
cube_santa_perm(n, inverse = FALSE)
}
\arguments{
\item{n}{Integer, the side of the cube (at least 2)}

\item{inverse}{Logical, return the permutation the other way round}
}
\value{
Integer vector of length \eqn{6n^2}
}
\description{
The permutation carrying a Santa 2023 sticker position to the package's own.
Santa numbers the faces \code{U F R B L D} and we number them
\code{U R F D L B}; within a face the two agree, so this permutation moves
whole faces and nothing else.
}
\examples{
p <- cube_santa_perm(3)
length(p)

# applying it twice with the inverse gets back where it started
identical(cube_santa_perm(3, inverse = TRUE)[p], seq_len(54L))
}
\seealso{
\code{\link{cube_santa}}, \code{\link{cube_santa_state}}
}
